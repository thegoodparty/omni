name: gp-sdk

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

# Cancel superseded PR runs: a new push makes the in-flight result irrelevant,
# and while cancel was off the new run also queued behind it. Never on main.
concurrency:
  group: gp-sdk-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  WORKSPACE: packages/gp-sdk

jobs:
  build:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup-node-workspace
        with:
          app: gp-sdk
          build-contracts: 'true'

      - name: Typecheck
        run: npm run typecheck -w ${{ env.WORKSPACE }}

      - name: Lint
        run: npm run lint -w ${{ env.WORKSPACE }}

      - name: Format check
        run: npm run format:check -w ${{ env.WORKSPACE }}

      - name: Build SDK
        run: npm run build -w ${{ env.WORKSPACE }}
