name: gp-webapp

on:
  pull_request:
    paths:
      - 'packages/gp-webapp/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/gp-webapp.yml'
      - '.github/actions/**'
  push:
    branches: [develop, qa, master]
    paths:
      - 'packages/gp-webapp/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/gp-webapp.yml'
      - '.github/actions/**'

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: gp-webapp-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  WORKSPACE: packages/gp-webapp

jobs:
  ci:
    name: Lint / Typecheck / Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup-node-workspace
        with:
          app: gp-webapp

      - name: Next typegen
        working-directory: ${{ env.WORKSPACE }}
        run: npx next typegen

      - name: Typecheck app
        working-directory: ${{ env.WORKSPACE }}
        run: npx tsc --noEmit

      - name: Typecheck e2e
        working-directory: ${{ env.WORKSPACE }}
        run: npx tsc --noEmit --project e2e-tests/tsconfig.json

      - name: Lint
        run: npm run lint -w ${{ env.WORKSPACE }}

      - name: Test
        run: npm run test -w ${{ env.WORKSPACE }}

  vercel-preview:
    name: Vercel preview/develop deploy
    needs: ci
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && github.ref_name == 'develop')
    runs-on: ubuntu-latest
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ vars.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ vars.VERCEL_PROJECT_ID_GP_WEBAPP }}
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup-node-workspace
        with:
          app: gp-webapp

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Build and deploy with Vercel CLI
        id: deploy
        shell: bash
        run: |
          vercel pull --cwd "$WORKSPACE" --environment=preview --yes --token "$VERCEL_TOKEN"
          vercel build --cwd "$WORKSPACE" --token "$VERCEL_TOKEN"
          DEPLOY_URL="$(vercel deploy --prebuilt --cwd "$WORKSPACE" --target=preview --token "$VERCEL_TOKEN" | awk 'NF { line=$0 } END { print line }')"
          case "$DEPLOY_URL" in
            http*) ;;
            *) DEPLOY_URL="https://${DEPLOY_URL}" ;;
          esac
          echo "url=${DEPLOY_URL}" >> "$GITHUB_OUTPUT"
          echo "gp-webapp preview URL: ${DEPLOY_URL}" >> "$GITHUB_STEP_SUMMARY"

      - name: Comment preview URL
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v8
        with:
          script: |
            const url = '${{ steps.deploy.outputs.url }}';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `gp-webapp Vercel preview: ${url}`
            });
