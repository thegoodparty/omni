name: contracts

on:
  pull_request:
    branches-ignore: [qa, master]
  push:
    branches: [develop, qa, master]

permissions:
  contents: read

concurrency:
  group: contracts-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

env:
  WORKSPACE: packages/contracts

jobs:
  build:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup-node-workspace
        with:
          app: contracts

      - name: Lint
        run: npm run lint -w ${{ env.WORKSPACE }}

      - name: Typecheck
        run: npm run typecheck -w ${{ env.WORKSPACE }}

      - name: Build contracts
        run: npm run build -w ${{ env.WORKSPACE }}

      - name: Publish note
        if: github.event_name == 'push' && github.ref_name == 'master'
        run: |
          echo "Contracts publish is intentionally not enabled in omni yet." >> "$GITHUB_STEP_SUMMARY"
          echo "Resolve publish ownership and npm token naming before enabling it." >> "$GITHUB_STEP_SUMMARY"
