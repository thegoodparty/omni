# Migration sync working area (never committed)
.sync-cache/

# Dependencies
node_modules/

# Build output
dist/
.next/
build/
coverage/

# Env (per-app .env files are intentionally untracked)
.env
.env.local
*.local

# Local agent context (personal, not shared)
CLAUDE.local.md

# Logs / OS
*.log
.DS_Store

.claude/worktrees
docs/superpowers

# Agent worktrees and scratch artifacts (local only)
.worktrees/
scratch/
.playwright-mcp/