NODE_ENV="test"
LOG_LEVEL="debug"
HOST="127.0.0.1"
PORT=3000
CORS_ORIGIN="http://localhost:3000"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/election_api_test"

# M2MAuthGuard verifies against this at request time; the Clerk verify call is
# stubbed in test-setup.ts so the integration harness authenticates as an M2M caller.
ELECTION_API_MACHINE_SECRET="ak_test"
