# Preview DB smoke test

Temporary PR to verify the shared preview Aurora cluster flow end-to-end
(per-PR `gpdb_pr_<n>` on `gp-api-preview-shared-db`, migrate + seed on startup)
and to measure preview deploy time. **Do not merge — close after testing.**
