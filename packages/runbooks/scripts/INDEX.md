# Script Index

Quick reference for all available scripts. Keep this updated when adding or removing scripts.

| Script                                          | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Used By                                                                                                                                                                                                                 |
| ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `python/databricks_query.py`                    | Execute SQL queries against Databricks and return results as a DataFrame (PAT auth)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | books/query-voter-data.md                                                                                                                                                                                               |
| `python/abridged_campaign_plan.py`              | Fake-it marketing test: generate short, personalized "abridged campaign plan" teasers from a CSV of contacts (name + location + optional office) via one Claude Sonnet call each, and write a CSV ready to merge into a HubSpot re-engagement email. Standalone; does NOT touch the production campaign-plan engine. Sample input at `python/fixtures/abridged_campaign_plan_sample.csv`; needs `ANTHROPIC_API_KEY`.                                                                                                                                                                                                                                                          | Standalone (marketing re-engagement experiment)                                                                                                                                                                         |
| `python/door_knocking_map_poc/`                | Door-knocking map POC — renders and filters a whole city's voter universe in the browser (binary parallel arrays + deck.gl; 460K Chicago dots phone-smooth, 14-dimension on-device filtering, polygon turf stats, live Geoapify route optimization). `./run.sh` fetches L2 data from Databricks with YOUR credentials into a gitignored `local/` — no voter data or keys are ever committed. See its README.md.                                                                                                                                                                              | python/door_knocking_map_poc/README.md                                                                                                                                                                                  |
| `python/resolve_ordinance_code.py`             | Resolve `(state, jurisdiction)` → authoritative municipal-code source with no web search: matches the Municode (`api.municode.com`) and General Code/eCode360 (`generalcode.com/text-library`) bulk client directories. Strips governing-body suffixes; stdlib-only; read-only. Tier 1 of the find-existing-ordinances workflow (misses fall to the web-search tier; a directory listing can be stale, so content-verify before relying on it).                                                                                                | books/find-existing-ordinances.md                                                                                                                                                                                       |
| `python/databricks_oauth.py`                    | `run_query(sql) -> DataFrame` against the SQL warehouse via the Databricks SDK OAuth profile (`~/.databrickscfg`, no PAT). OAuth counterpart to `databricks_query.py`; used by the event-health monitor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | books/monitor-analytics-event-health.md                                                                                                                                                                                 |
| `python/analytics_event_health.py`              | Event-health monitor (DATA-1952). Reconciles every Amplitude event across three axes (declared intent / code-presence provenance CSV / firing volume), classifies against the SOP status model, detects weekly firing-volume anomalies, tracks description-metadata completeness, and renders a severity-ranked digest appended to `instrumentation_data/analytics-event-health-log.md`. Curated `monitored_events.yaml` (now with a `dismissed:` section suppressing re-proposed events, set via the `/triage-instrumentation-gaps` skill; DATA-2152) drives elevation + the self-healing proposal queue. Pure logic unit-tested in `test_analytics_event_health.py`.                                                                                                                                    | books/monitor-analytics-event-health.md                                                                                                                                                                                 |
| `python/circle_query.py`                        | GET wrapper for the Circle Admin API v2 (Bearer auth). CLI prints JSON; `get()` helper for programmatic use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | books/connect-circle-api.md                                                                                                                                                                                             |
| `python/circle_engagement.py`                   | Full engagement snapshot — DAU/WAU/MAU, stickiness, contribution mix, content rate, top spaces/contributors, cohort retention                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | books/circle-engagement-snapshot.md                                                                                                                                                                                     |
| `python/clickup_api.py`                         | ClickUp API wrapper (GET/POST/PUT/DELETE) with token auth; v2 by default, `--api-version=v3` for the Docs/Pages API. CLI prints JSON; `get/post/put/delete()` helpers for programmatic use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | commands/clickup-epic-create.md, commands/clickup-epic-edit.md, commands/work-on-clickup.md                                                                                             |
| `python/qa_validate.py`                         | Validate a qa-spine-compliant artifact JSON: deterministic checks + Phase 1 LLM triage + Phase 2 adversarial escalation → release_verdict (ok/warn/block) + per-claim verdicts in qa_bundle.json. Product-agnostic; product-specific rules live in a `<product>_product_spec.json`.                                                                                                                                                                                                                                                                                                                                                                                         | books/qa-validate.md                                                                                                                                                                                                    |
| `python/eval_trajectory.py`                     | Trajectory eval — score an agent run's execution trace (`conversation.jsonl`, both Fargate + local dialects): turns, cost, tool_errors, exact-duplicate calls, planning overhead %, optional command-regex category breakdown. `--ab control_dir treatment_dir` diffs two prompt versions with an outcome-parity check.                                                                                                                                                                                                                                                                                                                                                     | books/evaluate-experiment-runs.md                                                                                                                                                                                       |
| `python/ab_savings.py`                          | A/B savings table — given a runs-map TSV (`exp, arm, path, label, run_id`), pulls each run's trace + artifact.json from S3 and prints per-input v1-vs-v2 cost/turns/planning with a **true-status** (not regex) outcome-parity check; pairs whose outcome diverges are flagged and excluded from the clean-pairs aggregate. Supports the **existing-data control** (historical runs on `ctrl` rows, fresh treatment on `treat` rows — apples-to-apples on inputs without re-running the control). `--verbatim PATH` also writes each input's full, untruncated control-vs-treatment artifact for a human quality read. The $-framed companion to `eval_trajectory.py --ab`. | books/evaluate-experiment-runs.md                                                                                                                                                                                       |
| `python/perf_gate.py`                           | Performance gate (objective head), **config-driven per experiment** — turns trajectory metrics + true artifact status into PASS / FLAG / FAIL. Universal hard FAIL = no artifact produced; per-experiment `fail_values` (status) + cost/turns/error ceilings come from `--config`. No universal status field (briefing_status / status / none), so the status field is configured per experiment. Pure `evaluate(metrics, status, config)` + CLI.                                                                                                                                                                                                                           | books/evaluate-experiment-runs.md, books/build-performance-rubric.md                                                                                                                                                    |
| `python/derive_perf_thresholds.py`              | Derive a per-experiment performance config (the "performance rubric") from real runs — samples deployed runs, discovers the status field, computes the cost/turns/error distribution, sets FLAG ceilings at p95, infers `fail_values`, records the no-artifact baseline. Pure measurement (no agent/judge). Emits `<exp>.perf.json` for `perf_gate.py --config`.                                                                                                                                                                                                                                                                                                            | books/build-performance-rubric.md                                                                                                                                                                                       |
| `python/perf_monitor.py`                        | Real-time performance monitor — pulls the latest N deployed dev runs, gates each against an adopted `perf.json`, and exits non-zero with `ALARM` when the no-artifact rate drifts above baseline (+10pts) or the FLAG rate spikes (>20%). Schedulable (loop/cron/launchd). Pure `drift_alarm()` + CLI.                                                                                                                                                                                                                                                                                                                                                                      | books/build-performance-rubric.md                                                                                                                                                                                       |
| `python/meeting_briefing_eval_rules.json`       | Example `--rules` file for `eval_trajectory.py` — command-regex → category labels (schema_read, qa, http_fetch, databricks, pdf_extract, artifact_build) for the meeting_briefing experiment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | books/evaluate-experiment-runs.md                                                                                                                                                                                       |
| `python/rubric_verdict.py`                      | Tally cold-judge rubric scores (TSV: `uuid	batch	judgeA	judgeB`, `DQ` for disqualified) into a GO/NO-GO **reliability** verdict — graded inter-judge spread, gate-decision reproducibility, blowout count. Reliability only, not validity. Input: the `rubric_scores.tsv` the build runbook produces (graduates to `experiment-evals/<exp>/` on adoption).                                                                                                                                                                                                                                                                                                                     | books/evaluate-experiment-runs.md                                                                                                                                                                                       |
| `python/faithfulness_check.py`                  | Offline external-faithfulness check (candidate Gate B3): extract identity figures (money, percent, legal cite, date) from each claim and string-match them against the cited source text. Default checks the artifact's embedded snapshot (catches naive fabrication); pass a resolver to check the runner-persisted raw download instead (gaming-resistant — catches a fabrication faked consistently across claim, extract, and snapshot). Reference implementation shaped to the meeting_briefing contract; adapt field names per experiment.                                                                                                                            | books/build-output-quality-rubric.md                                                                                                                                                                                    |
| `shell/coldrun-build-rubric.sh`                 | Fair-test loop: for each experiment, launch a context-free `claude -p` headless session that builds a quality rubric by following `books/build-output-quality-rubric.md` alone (barred from reading existing rubrics), writes to `$OUTROOT/<exp>/`, waits on all. Doubles as runbook validation — where a cold agent struggles is where the runbook is underspecified. Caller supplies `CLAUDE_CONFIG_DIR`/`AWS_PROFILE`.                                                                                                                                                                                                                                                   | books/build-output-quality-rubric.md                                                                                                                                                                                    |
| `python/verify_urls.py`                         | Verify a list of URLs return HTTP 200 (HEAD first, fallback to GET on 403/405/501). Follows redirects, returns `{url, status, final_url, ok, error?}` per row as JSON. Reads URLs from argv or stdin.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Standalone local/dev URL vetting only. NOT invoked by any experiment at runtime — agents verify URLs with `pmf_runtime.http.head` inside the sandbox (direct HTTP / this script would hang in the quarantined network). |
| `python/amplitude_event_provenance_backfill.py` | Amplitude event provenance engine. "walk" subcommand: git-history backfill/audit (used by books/refresh-event-provenance.md). "upsert" subcommand: single provisional-row write used by the instrument-analytics-event skill. Also records call-site count + retirement date (DATA-2046) from the EVENTS key-path, and instrumented/retired author email.                                                                                                                                                                                                                                                                                                                                                                                                                                               | books/refresh-event-provenance.md                                                                                                                                                                                       |
| `python/event_state_assembler.py`               | Joins the Amplitude catalog, provenance CSV, and `analytics_event_health` SOP status into the 16-column consumer event-state table. Output-agnostic; consumed by the gSheet sink. (DATA-2052)                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | books/refresh-event-state-surface.md                                                                                                                                                                                    |
| `python/event_state_gsheet.py`                  | Writes the assembler's rows to the `events` tab of a Google Sheet (full overwrite). OAuth via a cached token stored outside the checkout (`GP_SHEETS_TOKEN_PATH`, else `~/.config/gp-event-state`; DATA-2061) so it survives worktree removal; mint it once with `shell/mint-sheets-token.sh`. CLI: `refresh` / `--dry-run`. This is the consumer surface (a 472-row markdown table OOM'd the ClickUp doc). Also writes a derived `watchlist_status` column. Also writes a `meta` tab (last-refreshed timestamp + optional ClickUp backlink via `GP_EVENT_STATE_CLICKUP_URL`). (DATA-2052, DATA-2152)                                                                                                                                                                                                                                                                                                                                                                                                                       | books/refresh-event-state-surface.md                                                                                                                                                                                    |
| `python/segment_destination_mapping.py`         | Segment Public API wrapper for destination subscriptions (Action mappings): list destinations/subscriptions, show/create/update/enable a subscription. Creates subscriptions DISABLED (the Segment workspace is production); enabling is an explicit human step. Reads `SEGMENT_PUBLIC_API_TOKEN`. (DATA-2149)                                                                                                                                                                                                                                                                                                                                                          | .claude/skills/segment-event-to-hubspot/SKILL.md (repo root)                                                                                                                                                            |
| `python/hubspot_event_mapping.py`               | HubSpot API wrapper for the event-mapping pipeline: custom event definitions (list/create), contact-property similarity search (rapidfuzz) + creation, on-demand test occurrences (`/events/v3/send`), contact verification reads, Automation v4 flow get/create (flows created DISABLED). Targets the sandbox portal via `HUBSPOT_SANDBOX_TOKEN` by default; `--token-env` switches portals. (DATA-2149)                                                                                                                                                                                                                                                                | .claude/skills/segment-event-to-hubspot/SKILL.md (repo root)                                                                                                                                                            |
| `shell/generate-roadmap-pdf.sh`                 | Convert an EO's roadmap markdown variants to clean PDFs (pandoc + headless Chrome)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | books/generate-governing-roadmap.md                                                                                                                                                                                     |
| `shell/refresh-event-state.sh`                  | Refresh the event-state Google Sheet; funnels the manual + event-metadata + monitor triggers (DATA-2053). Resolves the cached token outside the checkout (`GP_SHEETS_TOKEN_PATH` / `~/.config/gp-event-state`, legacy in-checkout path still honored; DATA-2061), fetching client secrets from 1Password only when no token exists anywhere. Passes through --dry-run / --override / other event_state_gsheet.py refresh flags.                                                                                                                                                                                                                                                                                                                                                                                          | books/refresh-event-state-surface.md                                                                                                                                                                                    |
| `shell/mint-sheets-token.sh`                    | One-time Google OAuth consent for the event-state refresh (DATA-2061). Fetches client secrets from 1Password (or takes a saved secrets JSON as an arg), runs the browser consent, and caches the token outside the checkout so it survives worktree removal. Run once; then `refresh-event-state.sh` is non-interactive.                                                                                                                                                                                                                                                                                                                                                                                          | books/refresh-event-state-surface.md                                                                                                                                                                                    |
| `python/event_state_slack.py`                  | Push analytics event-lifecycle updates to a dedicated Slack channel (DATA-2057). Source A: real-time metadata-change posts (`notify-metadata` CLI, fired from the event-metadata skill). Source B: the delta-led health digest + threaded detail, quiet-gated, posted inline from `analytics_event_health.py --slack`. Shared bot token + `chat.postMessage` (threads the reply); pure formatters + gate unit-tested in `test_event_state_slack.py`. | .claude/skills/event-metadata, books/monitor-analytics-event-health.md |
| `python/digest_triage.py`                      | Rubric-guided priority triage for the governance Slack digest (DATA-2174): assembles one item per newsworthy/OKR-breaking event and judges it (Anthropic tool-call, degrades to the deterministic rules tier without `ANTHROPIC_API_KEY` or on judge failure) into a red/yellow/fyi tier + headline + action. Called by `analytics_event_health.py --slack` via `build_slack_triage`; skipped entirely when the quiet gate would suppress the post. Unit-tested in `test_digest_triage.py`. | books/monitor-analytics-event-health.md |
| `shell/notify-event-slack.sh`                   | Announce an event metadata change in Slack (DATA-2057, Source A). Terminal hook for the event-metadata skill; host-gates to a clean `exit 0` when the shared Slack bot credentials are absent. Passes through event_state_slack.py notify-metadata flags. | .claude/skills/event-metadata |
| `python/cap_cost_db.py`                         | Read-only prod gp-api DB connection for the CAP cost-analysis module — pulls `DB_PASSWORD` from Secrets Manager `GP_API_PROD` and connects to prod Aurora via pure-Python pg8000. `query(sql, **params) -> DataFrame`.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | analyze-cap-agent-costs skill                                                                                                                                                                                           |
| `python/cap_cost_scope.py`                      | Stage 1 scope resolver — English/filters (`--type`/`--on`/`--since`/`--last-cohorts`/`--run-ids`) → `experiment_run` rows, clustered into dispatch-window cohorts; prints count + UTC window and writes `scope.json`. Population, never sample.                                                                                                                                                                                                                                                                                                                                                                                                                             | analyze-cap-agent-costs skill                                                                                                                                                                                           |
| `python/cap_cost_extract.py`                    | Stage 2 per-turn extractor — fetches each run's `session.jsonl`, parses per-turn tokens, distributes the TRUSTED `costUsd` across turns by token weight (never token×list price), writes one cached `turns.parquet` + coverage. Reuses the `parse_session` loop adapted from the original analyze-experiment-costs parser (regex step-classifier dropped).                                                                                                                                                                                                                                                                                                                  | analyze-cap-agent-costs skill                                                                                                                                                                                           |
| `python/cap_cost_analyze.py`                    | Stage 3 analytics + matplotlib plots — distribution tables (median/p90/p99/max + Pareto tail per status), cumulative-cost curve, cost-velocity (spikes annotated by tool call), population heatmap keyed on normalized TURN progress (milestone primitive pending).                                                                                                                                                                                                                                                                                                                                                                                                         | analyze-cap-agent-costs skill                                                                                                                                                                                           |
| `python/cap_cost_hotspots.py`                   | Stage 4 hot-region detector — population-level hot turn-progress bands; `--label` calls the Anthropic SDK on only the hot slices to name what work is expensive there.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | analyze-cap-agent-costs skill                                                                                                                                                                                           |
| `python/cap_cost_profiles.py`                   | Stage 5 per-job profiles — `PROFILES` registry (status field, success status, outcome buckets, headline fn). Buckets outcomes from the S3 artifact; `meeting_briefing` headline = `sum(costUsd)/count(briefing_ready)` incl. failures.                                                                                                                                                                                                                                                                                                                                                                                                                                      | analyze-cap-agent-costs skill                                                                                                                                                                                           |
| `python/cap_cost_segment.py`                    | Stage 6 (opt-in) district-size segmentation — joins cohort orgs → Databricks `int__icp_offices.voter_count`, buckets `<10k/10k-50k/50k-100k/>100k`, reports cost per segment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | analyze-cap-agent-costs skill                                                                                                                                                                                           |
| `python/cap_cost_report.py`                     | Stage 7 self-contained HTML report — turns the stage 1/3/5 outputs (`scope.json`, `plots/distributions.json`, `profile.json`) + plot PNGs into ONE shareable, print-to-PDF `report.html` with every PNG embedded as a base64 `data:` URI and CSS inlined (no external URLs). Data-driven and degrades gracefully (missing milestone data, null headline, no profile). Reads `turns.parquet` for an optional cost-driver block.                                                                                                                                                                                                                                                | analyze-cap-agent-costs skill                                                                                                                                                                                           |
| `python/instrumentation_gaps.py`                | Instrumentation gap sweep (DATA-2151): enumerates gp-webapp/gp-api surfaces, diffs against tracking call sites, tracks dispositions, emits Slack run-data file and gsheet gaps tab, renders a ranked digest section. Supports `--seed` (one-off repo-wide audit) and `--load-seed` (load triage from review artifact). Phase 2 adds a graceful Anthropic (Sonnet) rubric-judgment pass over the untriaged candidate gaps (needs `ANTHROPIC_API_KEY`; skips cleanly without it — only judge-confirmed gaps enter state). Run by the analytics-governance cron alongside `analytics_event_health.py`. Dispositions for the weekly trickle are now set via the `/triage-instrumentation-gaps` skill using `--list-new` / `--review-artifact` / `--load-review` (DATA-2152), not by hand-editing the JSON.                                                                                                                                                                                                                                                                                                                                                                                                    | .github/workflows/analytics-governance.yml                                                                                                                                                                              |
| `/triage-instrumentation-gaps`                  | The ergonomic weekly review over two governance queues — instrumentation gaps in `instrumentation_gaps.json` and watchlist proposals in `monitored_events.yaml` (`events:`/`dismissed:`) — entered from the Slack digest, dispositioning each item and writing back via one PR; accept files a ClickUp ticket or hands off to `instrument-analytics-event` (DATA-2152). | .claude/skills/triage-instrumentation-gaps |
