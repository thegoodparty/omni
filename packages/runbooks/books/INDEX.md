# Book Index

Read this first when given a task. Match keywords to find the right book or command.

## Routing Table

| Type | Trigger Keywords | Procedure | Description |
|------|------------------|-----------|-------------|
| ref | platform, monorepo, omni, packages, gp-admin, gp-sdk, contracts, architecture, services, how services connect, codebases, infrastructure, AWS, ECS, RDS, S3, SQS, deployment, Pulumi, integrations, onboarding flow, path to victory, P2V, P2P, outreach, polling, data platform, dbt | books/platform-overview.md | Complete GoodParty tech ecosystem reference — codebases, service architecture, auth flows, AWS infrastructure, deployment, data pipelines, and end-to-end feature walkthroughs |
| proc | voter, haystaq, scores, flags, databricks, L2, voter data, quick query, issue scores | books/query-voter-data.md | Quick-query Haystaq voter data (scores, flags, demographics) via Databricks |
| ref | grafana, traces, metrics, alerts, tempo, prometheus, loki, spans, connection pool, histogram, alert history, TraceQL | books/query-grafana.md | Query Grafana Cloud for traces, metrics, and alert history via the API |
| proc | circle, community, engagement, members, posts, comments, social media, circle.so | books/connect-circle-api.md | Query the Circle Admin API v2 for community engagement — spaces, posts, comments, members |
| proc | dau, mau, wau, stickiness, retention, cohort, engagement snapshot, circle report, community health | books/circle-engagement-snapshot.md | Generate Circle community engagement snapshot — DAU/WAU/MAU, stickiness, contribution mix, cohort retention, top spaces/contributors |
| ref | evaluate pmf experiments, eval system, evals overview, how to evaluate, which eval runbook, start here evals, eval front door, decision brief, ship a prompt change, promote experiment, performance vs quality, two axes, gate an experiment, is this good enough | books/pmf-eval-system.md | FRONT DOOR for evaluating PMF experiments. Read this first for anything eval-related: it explains the whole system (the two axes — performance objective/blocks vs quality judgment/advises, where the standard lives in git vs runs in S3, the engine scripts) and routes to the build / evaluate / monitor runbooks. Ends in a decision brief: adopt, revert, or intervene. |
| proc | run pmf experiment, dispatch experiment, run in cloud, fargate, broker, smoke test experiment, dispatch SQS, monitor agent run, pull artifact, deploy experiment, broker rollover, cold start, run experiment dev | books/run-pmf-experiment-cloud.md | Run an EXISTING PMF experiment in the cloud: deploy-before-run matrix (republish instruction vs rebuild pmf-engine vs rebuild broker + wait for rollover), dispatch via SQS, confirm the Lambda launched a Fargate task, monitor runner/broker logs, fetch + validate the artifact from S3, and timing/cold-start. Companion to the build-cap-agent skill (which creates the experiment). |
| proc | build rubric, quality rubric, create rubric, generate rubric, output quality, score quality, cold judge, inter-judge agreement, reliability vs validity, held-out validation, rubric tuning, eligibility gate, discover dimensions, rubric_verdict | books/build-output-quality-rubric.md | Build and validate an output-quality rubric for ANY PMF experiment: collect sample artifacts (generate them by dispatching runs if none exist, seeding inputs from gp-api/Databricks), discover scoring dimensions from real outputs via parallel reader subagents, draft a gated rubric (faithfulness + eligibility gates, spine + anchored dimensions), tune with cold-judge subagents until independent judges agree, held-out validate, and produce a GO/NO-GO reliability verdict with `rubric_verdict.py`. Adopted rubrics graduate to experiment-evals/<exp>/ in their own PR. Reliability not validity; a GO is not a fact-check. |
| proc | performance rubric, performance gate, per-experiment thresholds, cost/turns/error ceilings, no-artifact failure, status field discovery, derive thresholds, real-time dev gate, deployed runs monitoring, objective eval head, onboard new experiment, add performance eval to a new experiment, day-one universal gate | books/build-performance-rubric.md | Autonomously derive a per-experiment performance rubric (objective head) from deployed runs and measure it in real time against dev. Covers onboarding a brand-new experiment (Phase 1 universal no-artifact gate from run one, Phase 2 tune once it has data). Discovers each experiment's status field (briefing_status / status / none — there is no universal one), sets p95 cost/turns/error FLAG ceilings from the experiment's own distribution, and treats "no artifact produced" as the one universal hard FAIL. Pure measurement (no judges, no validity caveat) via derive_perf_thresholds.py + perf_gate.py; adopted config graduates to experiment-evals/<exp>/perf.json. |
| proc | convert rubric to eval.md, qa gate, qa/eval.md, eval.md, in-run gate, gate check, editorial judge, fragment array, verdict.json, observe-only, rubric to gate, author qa eval, pass/fail rubric | books/convert-rubric-to-qa-eval.md | Convert an output-quality rubric (scored 1-5 OR pass/fail) into a `qa/eval.md` evaluator for an experiment's in-run QA gate. Routes each rubric criterion (eligibility gate / editorial judge check / deferred-to-main.py grounding / dropped fact-check), reshapes into the contract-C fragment-array output, writes `qa/manifest.json` (blocking:false), runs via a clean-context subagent, and validates with a dev smoke (placeholder vs scoreable run → inspect verdict.json). Template: `experiments/meeting_briefing/qa/eval.md`. |
| proc | trajectory eval, quality eval, assess agent runs, agent efficiency, wasted turns, planning overhead, redundancy, cost per run, A/B prompt, prompt optimization, eval harness, turns vs cost, conversation.jsonl, outcome parity | books/evaluate-experiment-runs.md | Assess PMF experiment runs on two axes — trajectory (turns/cost/errors/planning %/redundancy from conversation.jsonl) and quality (artifact vs rubric) — and A/B two prompt versions with the prompt as the only variable, gating turn/cost wins on outcome + quality parity. Uses scripts/python/eval_trajectory.py for trajectory and a cold-judge rubric (rubric_verdict.py) for quality. |
| proc | district issues, voter priorities, issue pulse, top concerns, news + voter data, find issues, haystaq + web, pair voter scores with news | books/find-district-issue-pulse.md | Given a state + district, find top 5 voter concerns (Haystaq scores) paired with one recent local news source per issue. Source runbook for the `district_issue_pulse` PMF experiment — workflow proven here before agent translation. |
| proc | election date, missing election dates, candidate election date, find election date, municipal election date, election calendar | books/find-election-dates.md | Recover missing election dates for a list of candidates: dedupe to distinct jurisdictions, optionally seed from election-api, fan out parallel web-research subagents, verify every source URL returns 200, emit a sourced date table keyed on the caller's id. |
| proc | ordinances, existing ordinances, municipal code, city code, code of ordinances, find ordinances, municode, ecode360, american legal, codifier, current law, ordinance source, elected official code | books/find-existing-ordinances.md | Locate and verify the authoritative source of an elected official's current municipal code (Municode/eCode360 client directories, then verified web search for American Legal / Code Publishing / encodePlus / city-hosted, with a bounded PDF content test before calling anything uncodified), guarding against same-name-different-place traps, and list its top-level chapters. Source runbook for the `find_existing_ordinances` PMF experiment (v5, certified n=300: 94% consolidated / 98% any form); the "current law" foundation for the Ordinances feature. |
| proc | opponent data, opponent scrape, ballotpedia, opponent website, opponent capture, know your opponent, web-only scrape, source url | books/find-race-opponent.md | Given an opponent name + race context, find their Ballotpedia page + campaign website, fetch both, and capture the page text as collected, tagged by source + the real fetched URL. As-collected capture only (no interpretation/scoring/comparison). Source runbook for the `race_opponent_collection` PMF experiment. |
| proc | opponent brief, opponent analysis, threat tier, why they're running, background, issues that matter, field analysis, SWOT, how your campaign stacks up, know your opponent brief, relative threat | books/find-race-opponent-analysis.md | Synthesize the v2 opponent brief (threat tier, overview, why they're running, background, issues that matter) from already-collected opponent text + the candidate's own bio/issues, plus one campaign-level `field_analysis` SWOT. Rich sources (`url`/`title`/`publisher`/`description`); descriptive sections sourced-or-silent, interpretive fields (`why_theyre_running`, `field_analysis`) unsourced; whole-field-at-once for relative tiers. Source runbook for the v2 brief redesign of the `race_opponent_summary` PMF experiment. |
| proc | stand out, action cards, opponent actions, sms contrast, preset sms, haystaq contrast, divergence issues, opponent summary to actions, know your opponent actions | books/find-race-opponent-actions.md | Turn persisted opponent summaries + the candidate's own platform into up to 5 "stand out" action cards (opponent, divergence issue, 3-sentence body citing district Haystaq sentiment, preset <=320-char first-person SMS), with a graceful numberless degrade when the district is missing or Haystaq has no coverage. Source runbook for the future `race_opponent_actions` PMF experiment. |
| exp | opposition research, opponents, who is running, candidates in race, opposition section, campaign plan opposition | experiments/opposition_research/ | CAP experiment that identifies the opponents in a candidate's race (full name, party affiliation, incumbent status) from the injected general + primary rosters plus a light web cross-check. Emits `{ "opponents": [...] }`; gp-api renders the section. Replaces the former `find-opposition-research.md` runbook. |
| exp | opportunities, challenges, strategic opportunities, campaign plan opportunities, campaign plan challenges, strategic landscape | experiments/opportunities_and_challenges/ | CAP experiment that produces the Opportunities and Challenges bullets for a campaign plan from the injected race numbers (projected votes needed to win, projected turnout, targeted voter contact goal, dates) plus web search. |
| proc | refresh provenance, event provenance, amplitude provenance, instrumentation history | books/refresh-event-provenance.md | Regenerate the committed Amplitude event git-provenance CSV (fallback/audit path) and open a PR. |
| proc | event state, event surface, refresh event table, analytics events doc | books/refresh-event-state-surface.md | Refresh the consumer-facing analytics event-state surface (the Google Sheet). |
| exp | race opponent collection, opponent data collection, opponent scrape, ballotpedia scrape, opponent website scrape, opponent capture, know your opponent, web-only opponent | experiments/race_opponent_collection/ | CAP web-only experiment that, given opponent names + race context, finds each opponent's Ballotpedia page + campaign website, fetches them via the broker, and emits one `{ opponent_name, source_type, source_url, content }` item per (opponent, source). No Databricks/voter scope, no interpretation. Thin precursor to opponent profiling; paired with `books/find-race-opponent.md`. |
| proc | prd, product spec, tech design, architecture options, design doc, technical approach, bless architecture, options + tradeoffs, drawio, data flow diagram, multi-repo | commands/prd-to-tech-design.md | Convert a PRD into a blessed tech design doc + drawio data flow diagram, published as a ClickUp page under the PRD. Multi-repo recon, architecture options + tradeoffs, four required sections (I/O, DB, DR, diagram). Also `/prd-to-tech-design`. |
| proc | clickup, epic, design doc, breakdown, create epic, agent-ready tasks, ticket generation, work breakdown | commands/clickup-epic-create.md | Take a (blessed) tech design + repo and break the work into a ClickUp Epic with N agent-ready subtasks (context, impl details, AC, test plan). Redirects to `commands/prd-to-tech-design.md` if input is a PRD. Also `/clickup-epic-create`. |
| proc | clickup, edit epic, restructure, add task, remove task, archive task, change priority, dependency, snapshot diff | commands/clickup-epic-edit.md | Edit an existing ClickUp Epic and its subtasks via a snapshot/diff/apply flow — add, remove, edit, change priorities or dependencies. Default-archive on removals. Also `/clickup-epic-edit`. |
| proc | clickup, work on task, pick up ticket, implement task, claude code task, ac, acceptance criteria, scope confirmation, code review subagent, independent review, delegate bar, delegate first try, playwright ui tester, review loop, severity gate | commands/work-on-clickup.md | Pull a ClickUp task, load its Epic plan, scope-confirm with explicit options (`go`/`plan`/`focus`/`split`), then implement it yourself and run two read-only subagents on the cumulative diff for the review the implementer can't give itself: `gp-reviewer` (independent review that mirrors the delegate-reviewer bot's blocker bar across correctness/security/tests/conventions/ai-rules/cross-file/thematic, so delegate passes first try) and, for UI work, `gp-ui-tester` (real browser via Playwright MCP). Loop on blocker/major findings in-context until convergence or a small cap, then verify AC and optionally update the ticket. Front-loads what the delegate-reviewer bot gates at PR time. Also `/work-on-clickup`. |
| proc | clickup, work on epic, run epic, whole epic, orchestrate epic, parallel tickets, dependency graph, phases, autonomous, controlled mode, automated mode, worktree per ticket, auto-merge, loop until done, epic playwright | commands/work-on-epic.md | Take a ClickUp Epic, derive a dependency-aware execution plan across its subtasks, then drive each ticket through implement → PR → merge → done (parallel where safe, phased where not) using the work-on-clickup flow + ship-pr per ticket in its own worktree, looping until the Epic is closed, then firing a Playwright (MCP) pass over the delivered behavior. Runs controlled (recommended — per-ticket `go` check-ins, user merges each PR) or automated (one plan-approval gate, then unattended with auto-merge); pulls the integration branch into open worktrees after each merge. Also `/work-on-epic`. |
| proc | release, release prep, pending production release, develop to qa, qa to master, qa to prod, devs-only, slack release confirmation, omni release, gp-ai-projects release, multi-repo release | commands/release-prep.md | For each release repo (omni `develop → qa → master`, gp-ai-projects `develop → qa → prod`), open the `develop → qa` PR with auto-merge enabled, poll for it to merge, then open the pending `qa → production` PR. Print one combined `#devs-only` message grouping included PRs per repo then by author for team sign-off. Also `/release-prep`. |
| proc | release, production deploy, ship, qa to master, qa to prod, product-releases, release notes, deploy to prod, omni release, gp-ai-projects release, multi-repo release | commands/release.md | Merge each release repo's open `qa → production` PR (omni → master, gp-ai-projects → prod; works standalone — no prior `/release-prep` required), wait 5 minutes for deploys, then print one combined `#product-releases` message listing every released ticket (ENG-/DATA-/WEB-/CAP-/DT-) with its ClickUp title (plus untagged fallbacks). Also `/release`. |
| proc | qa, validate, qa_validate, release verdict, qa_bundle, deterministic checks, claim triage, phase 1, phase 2, product spec, qa-spine | books/qa-validate.md | Validate a qa-spine-compliant artifact JSON: deterministic checks + Phase 1 LLM triage + Phase 2 adversarial escalation → `release_verdict` ∈ {ok, warn, block}. Product-agnostic; product-specific rules live in a `<product>_product_spec.json`. |
| proc | roadmap, governing roadmap, elected official, EO, D-Long, D-Short, tactical action plan, strategic roadmap, AM summary, generate roadmap | books/generate-governing-roadmap.md | Generate a verified, PDF-ready strategic governing roadmap package (D-Long, D-Short, internal AM summary) for an elected official |
| proc | first roadmap, quickstart, getting started, how to run roadmap, account manager, self-serve, run a roadmap | books/run-your-first-roadmap.md | Plain-language quickstart for requesting a governing roadmap via Claude Code: what to provide, what you get, and how to review before sending |
| ref | roadmap example, sample roadmap, what does a roadmap look like, example package, D-Short example, AM summary example | books/example-roadmap-package.md | Fabricated worked example of a finished roadmap package (D-Short, D-Long outline, AM summary) for reference |
| ref | scoring, rubric, 12 dimensions, SEND, REVIEW, REWRITE, combined score, calibration, roadmap quality check | books/roadmap-scoring-rubric.md | Internal quality-check aid for governing roadmaps: 12-dimension rubric, verdict thresholds, and calibration. Backstop to accuracy verification, not the main deliverable |
| proc | analytics health, event health, monitor events, flatline, event drop, broken tracking, amplitude monitoring, tracking regression, registration broke, orphaned event, dormant event, event metadata completeness, win serve events, DATA-1952 | books/monitor-analytics-event-health.md | Reconcile every Amplitude event across three axes — declared intent (gp-meta), code presence (provenance CSV), and firing volume (catalog + raw stream) — classify each against the SOP status model, detect firing-volume anomalies, track metadata completeness, and append a severity-ranked digest to the longitudinal log. Curated watchlist drives elevation + self-healing proposals; produces a remediation payload for the event-metadata skill. |

## Types

- **proc** — step-by-step procedure for accomplishing a task
- **ref** — informational reference for lookup and context
- **exp** — a deployed CAP / PMF experiment (`experiments/<id>/`); the `instruction.md` the cloud agent runs. See `books/run-pmf-experiment-cloud.md` to dispatch one.

## Where procedures live

- **`books/`** — procedures the agent reads and follows when asked. No install needed.
- **`commands/`** — procedures that *also* register as Claude Code slash commands (via `./install.sh`). The file content IS the procedure — agents can read it directly the same way they read books, plus invoke via `/<name>`.

Both are markdown procedures with the same shape. The split is about invocation surface, not content.

## Quick Decisions

```
No match? → Ask the user or proceed without a book
```
