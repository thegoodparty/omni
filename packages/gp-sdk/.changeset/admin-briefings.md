---
'@goodparty_org/sdk': minor
---

Add `client.admin.briefings` admin sub-resource with `list(params)` and `get(id)` for meeting briefings, plus `BriefingAdminRow`, `BriefingAdminListQuery`, and `BriefingAdminDateRange` types.
