// These shapes are owned by @goodparty_org/contracts (the cross-service source
// of truth). Re-exported here so SDK consumers keep importing them from the SDK
// surface without the SDK redefining — and drifting from — the canonical types.
export type {
  RaceTargetMetrics,
  CampaignWithPositionName,
  CampaignWithLiveContext,
} from '@goodparty_org/contracts'
