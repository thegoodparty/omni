import type { PrototypeMeta } from '@/shared/prototypeMeta'

const meta: Omit<PrototypeMeta, 'slug'> = {
  title: 'Serve Nav Kit',
  description:
    'Port of the snuggle-nav-kit Lovable app (Campaign / Win product) onto the GoodParty styleguide. Surface-level shell first, then depth per subsection.',
  author: 'eugene.bazdyriev@goodparty.org',
  createdAt: '2026-07-21',
  status: 'draft',
}

export default meta
